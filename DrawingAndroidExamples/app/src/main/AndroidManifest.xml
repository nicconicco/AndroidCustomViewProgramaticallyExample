<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nicco.drawingandroidexamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrawingAndroidExamples">
        <activity
            android:name=".others.CircleDrawingActivity"
            android:exported="true" />
        <activity
            android:name=".others.DrawViewActivity"
            android:exported="true" />
        <activity
            android:name=".others.MyDrawableActivity"
            android:exported="true" />
        <activity
            android:name=".others.CustomDrawableActivity"
            android:exported="true" />
        <activity
            android:name=".test.FinalComponentActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>